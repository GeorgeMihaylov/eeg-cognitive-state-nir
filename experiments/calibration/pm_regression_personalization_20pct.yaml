experiment:
  name: pm_regression_personalization_20pct
  type: pm_regression_personalization
  output_dir: ./benchmark_results/pm_regression_personalization_20pct
  split_seed: 42
  model_seed: 42
  require_cuda: true
  resume: true

base_run:
  config_path: ./experiments/calibration/pm_regression_personalization_20pct_base.yaml
  dataset: emotiv_pm_regression
  task: performance_metrics_regression
  model: torch_mlp
  train_if_missing: true

targets:
  - target_attention
  - target_engagement
  - target_excitement
  - target_stress
  - target_relaxation
  - target_interest
  - target_focus

calibration:
  maximum_calibration_fraction: 0.20
  budgets: [0.20]
  methods:
    - zero_shot
    - bias_correction
    - affine_calibration
    - head_only
    - full_model
  adaptation_validation_fraction: 0.20
  fraction_allocation: global_prefix
  window_seconds: 10.0
  max_gap_seconds: 10.5
  affine_alpha: 1.0
  minimum_calibration_samples: 5
  minimum_adaptation_train_samples: 4
  minimum_evaluation_samples: 20
  method_params:
    head_only:
      max_epochs: 5
      learning_rate: 0.001
      weight_decay: 0.0001
      early_stopping_patience: 3
    full_model:
      max_epochs: 5
      learning_rate: 0.0001
      weight_decay: 0.0001
      early_stopping_patience: 3

statistics:
  bootstrap_samples: 1000
  bootstrap_seed: 42
